import { CommentLogPipe } from './comment-log.pipe';

describe('CommentLogPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentLogPipe();
    expect(pipe).toBeTruthy();
  });
});
